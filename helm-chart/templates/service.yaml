apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name }}-service
spec:
  type: LoadBalancer
  selector:
    app: {{ .Values.name }}
  ports:
  - name: http
    port: 80
    targetPort: {{ .Values.port }}